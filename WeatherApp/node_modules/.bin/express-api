#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../express-generator-api-es6/bin/express-api" "$@"
  ret=$?
else 
  node  "$basedir/../express-generator-api-es6/bin/express-api" "$@"
  ret=$?
fi
exit $ret
